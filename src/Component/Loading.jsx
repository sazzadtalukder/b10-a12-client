import React from 'react';

const Loading = () => {
    return (
        <div>
            <span className="loading loading-spinner loading-xl"></span>
        </div>
    );
};

export default Loading;